//Given four integers,
//public int solution(int A, int B, int C, int D){
//	-google 1point3acres
//}
//变换他们的位置，使得，F(S) = abs(S[0] - S[1]) + abs(S[1] - S[2]) + abs(S[2] - S[3]
//	) 最大。其中，S[0]，S[1]，S[2]，S[3]就是ABCD要对应的位置。
// all  the codes are written by myself.
//					--Changyun Gong
#include <iostream>
#include <math.h>
#include <vector>

using namespace std;

int solution(vector<int> &a){
	if (a.size() != 4){
		cout << "error! size" << endl;
		return -1;
	}
	11
}

int main(int argc, char ** argv){
	vector<int> S;
	S.push_back(2);
	S.push_back(7);
	S.push_back(8);
	S.push_back(1);
	solution(S);
	getchar();
}